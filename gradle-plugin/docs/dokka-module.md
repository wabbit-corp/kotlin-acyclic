# Module kotlin-acyclic-gradle-plugin

Typed Gradle integration for the `one.wabbit.acyclic` compiler plugin.

It wires the compiler plugin artifact into Kotlin JVM and Kotlin Multiplatform compilations and forwards `acyclic {}` settings as compiler-plugin options.
